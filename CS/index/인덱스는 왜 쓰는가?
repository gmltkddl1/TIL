DB는 데이터를 페이지라는 고정된 크기의 블록으로 데이터를 읽고 저장한다
이 중 행 하나만이라도 읽더라도 페이지 전체를 메모리에 올려야한다
인덱스가 없다면 데이터를 조회할때 전체 페이지를 모두 메모리에 올리고 확인해야함
데이터가 100만 페이지라면 전체를 읽어야 할수도 있음
인덱스가 있으면 3번 4번 정도의 읽기로 해당 페이지를 가져올수 있음

즉 인데스의 목적은 디스크 I/O를 줄이는 것이다
